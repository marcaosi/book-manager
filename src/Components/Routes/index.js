import React from 'react'
import { BrowserRouter, Route } from 'react-router-dom'

export default () => {
    return (
        <BrowserRouter>
            <Route path="/" exact>

            </Route>
        </BrowserRouter>
    )
}